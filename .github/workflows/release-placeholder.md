# Release notes placeholder

Before publishing to GitHub, consider:

- setting the repository URL in `Cargo.toml`
- confirming the license/copyright holder
- tagging a release (`v0.1.0`)
- optionally attaching release binaries built with `cargo build --release`
